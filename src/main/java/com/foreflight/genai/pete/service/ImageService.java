package com.foreflight.genai.pete.service;

import org.springframework.stereotype.Service;

@Service
public class ImageService {

//    @Autowired
//    private DriverClient driverClient;
//
//    public byte[] getPngImage(String imageId) {
//        return imageClient.getPngImage(imageId);
//    }
//
//    public ImageResolution getImageResolution(String imageId) throws IOException {
//        byte[] imageBytes = getPngImage(imageId);
//        try (ByteArrayInputStream bais = new ByteArrayInputStream(imageBytes)) {
//            BufferedImage bufferedImage = ImageIO.read(bais);
//            return new ImageResolution(bufferedImage.getWidth(), bufferedImage.getHeight());
//        }
//    }
}